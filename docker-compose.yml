version: '3.8'

services:
  redis:
    image: redis:7.2-bookworm
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    
    volumes:
      - redis-data:/data

volumes:
  redis-data:
    driver: local
  
