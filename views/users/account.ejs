<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
   <link rel="stylesheet" href="/css/account.css">

  <%- include('../partials/head') %>
</head>
<body>
    
<%- include('../partials/header') %>
<main class="account-wrap">
  <section class="account-card">
    <% if (authz.isAdmin(user)) { %>
    <span >
      <a class="btn accent" href="/adminPage" style="float: right;">
        Zur Admin Seite
    </a>
    </span>
    <% } %>
    <h2>Mein Account</h2> 
    
     


    <div class="account-info">
      <p><strong>Benutzername</strong></p>
      <p class="account-value"><%= user.username %></p>
      
    </div>

     <div class="account-info">
      <p><strong>Rolle</strong></p>
      <p class="account-value"><%=user.role %></p>
      
    </div>

    <div class="account-info" hx-get="/users/points" hx-trigger="load" hx-target="#points-value" hx-swap="innerHTML">
      <p><strong>Punkte</strong></p>
      <p class="account-value account-points-shell" id="points-value" aria-live="polite">Wird geladen...</p>
    </div>

    <form class="account-form" action="/users/account/update-username" method="POST">
      <label for="newUsername">Neuer Benutzername</label>
      <input id="newUsername" name="username" type="text" placeholder="Neuer Benutzername" required>
      <button type="submit" class="btn">Speichern</button>
    </form>

    <form class="account-form" action="/users/account/update-password" method="POST">
      <label for="currentPassword">Aktuelles Passwort</label>
      <input id="currentPassword" name="currentPassword" type="password" placeholder="Aktuelles Passwort" required>

      <label for="newPassword">Neues Passwort</label>
      <input id="newPassword" name="newPassword" type="password" placeholder="Neues Passwort" required>

      <button type="submit" class="btn warning">Passwort ändern</button>
    </form>

    <div class="account-actions">
      <a class="btn outline" href="/">Zur Startseite</a>
      <a class="btn logout" href="/users/logout">Abmelden</a>
    </div>
  </section>
</main>

<%- include('../partials/footer') %>
</body>
</html>


